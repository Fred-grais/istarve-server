<%= form_for(@restaurant_rating) do |f| %>
  <% if @restaurant_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurant_rating.errors.count, "error") %> prohibited this restaurant_rating from being saved:</h2>

      <ul>
      <% @restaurant_rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="field">
    <%= f.label :restaurant_id %><br>
    <%= f.number_field :restaurant_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
